name: Generar 300 publicaciones premium SEO

on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/generar-150-posts.mjs'
      - '.github/workflows/generar-150-posts.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-posts:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4
      - name: Preparar Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Generar publicaciones premium, índices, tags y sitemap
        run: node scripts/generar-150-posts.mjs
      - name: Publicar cambios generados
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add posts data sitemap.xml robots.txt assets/img/posts tecnologia pan-y-circo alimentacion deportes matrix saliendo-de-la-matrix tags
          git commit -m "Generar 300 publicaciones premium SEO" || echo "Sin cambios para publicar"
          git push
